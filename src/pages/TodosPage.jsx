import React from "react";
import TodosList from "../components/todos/TodosList";

const TodosPage = () => {
  return (
    <div>
      <TodosList title={"Todos List"} />
    </div>
  );
};

export default TodosPage;
